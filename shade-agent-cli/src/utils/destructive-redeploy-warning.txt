DESTRUCTIVE ACTION — DEPLOY_CUSTOM REDEPLOY

State to be destroyed at: <ACCOUNT_ID>

Because deploy_custom is set to true and the account already
exists, the CLI will CLEAN this account by deploying a state 
cleaner contract to it, then redeploy the new contract.

This will permanently destroy:
  - All on-chain state stored by the contract (including any
    tracked balances, registrations, configs, or other persisted
    data).

Before continuing, confirm that:
  - The contract holds no valuable state you need to preserve.

It is also strongly recommended that:
  - You migrate all assets associated with the account -
    including FTs, NFTs, and other valuable state - to a new
    account before continuing.

By typing "yes" you acknowledge the loss of all account state 
and potential loss of assets and choose to proceed with the redeploy.

You can alternatively redeploy the contract to a new account.

Type "yes" to confirm:
